@model Lab3.Models.Task1ViewModel

@{
    ViewData["Title"] = "Завдання 1";
}
<br/>
<h4>
    Прізвища та назви груп студентів з оцінками <b>@(Model.MarkString == "Усі оцінки" ? "" : $"{Model.MarkString}")</b> із заданого предмета.
</h4>
<br/>
<div style="display: flex">
    <div style="width: 55%; margin-right: 50px">
        <table class="table table-striped">
            <thead>
            <tr>
                <th>Прізвище</th>
                <th>Група</th>
                <th>Оцінка</th>
                <th/>
            </tr>
            </thead>
            <tbody>
            @foreach (var item in Model.Tuple!)
            {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Group)
                </td>
                <td>
                    @(item.Mark?.ToString() ?? "0")
                </td>
                <td/>
            </tr>
            }
            </tbody>
        </table>
    </div>
    <div style="width: 45%; padding-left: 30px; padding-top: 20px;  padding-right: 100px">
        <form asp-action="Index">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="MarkString" class="control-label">Вибрати оцінку:&nbsp; </label>
                <select asp-for="MarkString" asp-items="Model.Marks"></select>
                <span asp-validation-for="MarkString" class="text-danger"></span>
            </div>
            <br/>
            <div class="form-group">
                <input type="submit" value=" Оновити " class="btn btn-outline-dark"/>
            </div>
        </form>
    </div>
</div>